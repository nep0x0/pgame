using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class latihan2 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Halo dunia dari unity");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
